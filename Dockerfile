FROM node:21-alpine
# Make a /home/node/todo-api directory and copy needed files into it
#   (lib, static, and package.json)
# Change ownership of everyhting in /home/node/todo-api to node user/group
# Make /home/node/todo-api your working directory
# Switch to the node user
# Install all libraries
# Open port 8000
# Set command to run project
